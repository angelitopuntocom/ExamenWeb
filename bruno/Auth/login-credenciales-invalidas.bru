meta {
  name: Login Credenciales Invalidas
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "noexiste@miapp.com",
    "password": "wrongpass"
  }
}

tests {
  test("Devuelve 401", function() {
    expect(res.status).to.equal(401);
  });
}